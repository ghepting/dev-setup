#!/bin/bash
if [[ "$*" == *"--version"* ]]; then
  echo "Python 3.14.2-mock"
elif [[ "$*" == *"pip --version"* ]]; then
  echo "pip 24.0 from /mock/site-packages (python 3.14)"
else
  echo "MOCKED: python $*"
fi
exit 0
